/// Match & Void session ruleset.
enum MatchGameMode {
  /// Clear 10 boards (void or match) with score penalties.
  arena,

  /// Score as many points as possible before time runs out.
  countdown,
}
